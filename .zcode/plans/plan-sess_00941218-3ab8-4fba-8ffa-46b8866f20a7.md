# Implementation Plan: Strip recruitment backend, keep clean admin panel

## Goal
`C:/laragon/www/izsuite-admin` theke recruitment backend (Recruiters, Candidates, Affiliates, Benefits, Credit Packs, How It Works, Tour Guide, Testimonials, Companies, SMTP, Experience Levels, Activity Logs UI, Marketing Leads, Mail Batches) + puro public/recruiter/candidate/affiliate API remove korbo. Sudhu admin panel modules + Auth + Admin Profile + public Contact API rekhe dibo.

## Final Decisions
- KEEP: Overview, Staff, Roles, Tickets, Departments, Plans, Subscriptions, Payment Methods, FAQs, Contact Messages, Header/Footer (content), Pages, Languages, Currencies, Settings, Auth, Admin Profile, **ActivityLog engine (Service/Model/Middleware/repo/migration/seeder)**, **Departments**, **Public Contact API**, **Integration orphan files (untouched)**.
- Activity Logs: only UI remove (controller + views + sidebar + route).
- Migrations: KEEP all (no file delete, no schema change) — DB consistent thakbe.
- User model: remove only relationship methods + fillable/cast entries referencing deleted models. No migration edit.

---

## STEP 1 — Edit `routes/admin.php`
Remove these blocks + unused `use` statements at top:
- Affiliates (lines 38–47), Recruiters (54–64), Candidates (66–76)
- How It Works (83), Tour Guides (84), Testimonials (85), Benefits (87), Experience Levels (88), Companies (89)
- Activity Logs (102), Credit Packs (110), SMTP (113–115)
- Marketing Leads (131–135), Mail Batches (138–141)
- Remove unused imports: AffiliateController, UserController, BenefitController, CompanyController, CreditPackController, ExperienceLevelController, HowItWorkController, TestimonialController, TourGuideController, MarketingLeadController, MailBatchController, SmtpController, ActivityLogController.
- KEEP: Overview, Profile, Staff, Roles, Pages, Departments, Faqs, ContactMessages, Content, Tickets, Subscriptions, Plans, PaymentMethods, Languages, Currencies, Settings, switch-language/currency.

## STEP 2 — Edit `routes/web.php`
- Remove `isAffiliate()` home redirect branch (14–16)
- Remove Demo routes (32–42)
- Remove `require affiliate.php` (45)
- KEEP admin redirect + admin.php require

## STEP 3 — Replace `routes/api.php`
Reduce to ONLY public contact-message routes (lines 23–24). Delete all other imports and routes (pricing, credits, tour-guides, jobs, payments webhook, auth block).

## STEP 4 — Edit `bootstrap/app.php`
- Remove `require candidate.php`, `require recruiter.php`, `require admin-api.php` from `then:` closure (lines 31–33)
- Remove `affiliate` middleware alias (line 40)

## STEP 5 — Edit `routes/console.php`
- Remove both scheduled entries (extract-pending-resumes, SendWeeklyRecruitmentReports). KEEP `inspire`.

## STEP 6 — Edit `resources/views/components/admin-sidebar.blade.php`
Remove `<a>` blocks + reposition section titles (Management / Frontend CMS / System) so kept links stay organized:
- Remove: Recruiters, Candidates, Affiliates, Benefits, Experience Levels, Activity Logs, Marketing Leads, Credit Packs, How It Works, Tour Guide, Testimonials, Companies, SMTP.
- KEEP under Management: Departments, Plans, Subscriptions, Payment Methods, FAQs, Contact Messages.
- KEEP under Frontend CMS: Header & Footer, Pages.
- KEEP under System: Languages, Currencies, Settings.

## STEP 7 — Edit `resources/views/admin/overview/index.blade.php`
Remove/replace quick-link cards referencing recruiters (line 25), candidates (35), activity-logs (55). Replace with neutral cards (Departments, Plans, Tickets, etc.) or just remove.

## STEP 8 — Edit KEEP-side cross-cutting code
- `app/Http/Controllers/Admin/SubscriptionController.php` — remove `role.slug=recruiter` and `plan_for=recruiter` filters in `create()`.
- `app/Repositories/UserRepository.php` — remove affiliate/recruiter/candidate methods (`getPaginated` affiliate eager-loads, `getAffiliates`, `getRecruitersAndCandidatesCount`, `referredByAffiliate` eager-loads). KEEP methods used by OverviewService (`getTotalCount`, `getActiveCount`, `getGrowthHistory`).
- `app/Repositories/PlanRepository.php` — remove `plan_for=recruiter` scopes (lines 40, 51).
- `app/Models/User.php` — remove fillable entries (`credit_balance`, `verification_otp`, `verification_otp_expires_at`, `affiliate_*`), matching casts, and relationship methods (`creditTransactions`, `socialAccounts`, `affiliateReferrals`, `referredByAffiliate`, `affiliateReferralInvoices`, `recruiterNotifications`), and `isAffiliate()`. KEEP role/tickets/subscriptions/invoices methods.

## STEP 9 — Delete recruitment-domain files & directories

**Entire directories:**
- `app/Http/Controllers/Api/` (EXCEPT `ContactMessageController.php`)
- `app/Http/Controllers/Affiliate/`
- `app/Http/Controllers/Demo/`
- `app/Services/Affiliate/`
- `app/Services/AI/`
- `app/Services/Api/Admin/`, `Api/Auth/`, `Api/Candidate/`, `Api/Profile/`, `Api/Public/`, `Api/Recruiter/` (EXCEPT `Api/ContactMessages/`)
- `app/Services/Recruiter/`
- `app/Services/Payments/`
- `app/Jobs/` (entire dir — all recruitment)
- `app/Console/Commands/` (entire dir)

**Route files:** `routes/recruiter.php`, `routes/candidate.php`, `routes/affiliate.php`, `routes/admin-api.php`

**Admin controllers:** `AffiliateController.php`, `UserController.php`, `BenefitController.php`, `CompanyController.php`, `CreditPackController.php`, `ExperienceLevelController.php`, `HowItWorkController.php`, `TestimonialController.php`, `TourGuideController.php`, `MarketingLeadController.php`, `MailBatchController.php`, `ActivityLogController.php` (UI only), `SmtpController.php`.

**Admin services:** `Admin/BenefitService.php`, `CandidateActivityOverviewService.php`, `CompanyService.php`, `CreditPackService.php`, `ExperienceLevelService.php`, `HowItWorkService.php`, `MarketingLeadService.php`, `RecruiterActivityOverviewService.php`, `ReportService.php`, `TestimonialService.php`, `TourGuideService.php`, `UserService.php`.

**Models:** Applicant, ApplicantAnswer, Benefit, Category, Company, CreditPack, CreditPurchase, CreditTransaction, ExperienceLevel, HowItWork, Integration(decision: KEEP per your non-selection), Interview, JobListing, JobQuestion, MarketingLead, MailBatch, MailLog, OfflineReceipt, Optimization, RecruiterNotification, Resume, SocialAccount, Testimonial, TourGuide, AffiliateReferral, UserConnection.

**Repositories:** ApplicantAnswerRepository, ApplicantRepository, BenefitRepository, CategoryRepository, CompanyRepository, CreditPackRepository, CreditPurchaseRepository, CreditTransactionRepository, ExperienceLevelRepository, HowItWorkRepository, InterviewRepository, JobListingRepository, JobQuestionRepository, MailBatchRepository, MarketingLeadRepository, OfflineReceiptRepository, OptimizationRepository, RecruiterNotificationRepository, ResumeRepository, SocialAccountRepository, TestimonialRepository, TourGuideRepository, UserRepository(if unused) — KEEP UserRepository (edited in Step 8).

**Requests (app/Http/Requests/Admin/ + subdirs):** all `*Benefit*`, `*Company*`, `*CreditPack*`, `*ExperienceLevel*`, `*HowItWork*`, `*MarketingLead*`, `*Smtp*`, `*Testimonial*`, `*TourGuide*`, `*StoreUser*`, `*UpdateUser*` request files.

**Mail:** `MarketingEmail.php`, `RecruiterNotificationMail.php`.
**Notifications:** `GuestNotification.php`.
**Views:** `resources/views/admin/{affiliates,users,benefits,experience-levels,activity-logs,marketing-leads,mail-batches,credit-packs,how-it-works,tour-guides,testimonials,companies,smtp}/` directories.
**Seeders:** `CandidateSeeder.php`, `CategorySeeder.php`, `FeatureToggleSeeder.php`, `NotificationSeeder.php`. EDIT `DatabaseSeeder.php` to drop these calls.

**Migrations:** NOT deleted (per decision — files stay).

## STEP 10 — Verify & smoke test
- `php artisan route:list` — confirm no `admin.recruiters/candidates/affiliates/...` routes remain; confirm keep routes intact.
- `php artisan config:clear && php artisan cache:clear`
- `php -l` syntax check on edited files (admin.php, web.php, api.php, bootstrap/app.php, console.php, User.php, UserRepository.php, PlanRepository.php, SubscriptionController.php).
- Note: cannot run full migrations/serve without DB; will report what was verified.

## Notes / What I will NOT touch
- Migrations (DB schema unchanged).
- `Integration` model/controller/views (orphan, you didn't select for removal).
- `PasswordResetOtp`, `PasswordResetOtpMail`, `VerifyEmailOtpMail` — conservative keep (Auth module retained; verifying deep web-auth usage is out of scope).
- `Sanctum`/`personal_access_tokens` migration — kept.
- Billing migrations 0031–0037 — kept to avoid schema drift.

## Risk handling
If any deleted-file reference is discovered during verification (e.g. a seeder imports a deleted model), I will fix that reference too. I will run the route:list + syntax checks before declaring done, and report any remaining issues honestly.